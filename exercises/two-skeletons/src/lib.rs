//! Pre-generated results for the "One Task, Two Skeletons" exercise.
//!
//! Both modules implement the same task: record a vehicle test session,
//! add distance samples, mark completion, and report total distance in
//! km. Module `a` fills in Skeleton A the way an agent typically does.
//! Module `b` fills in Skeleton B. The tests in `a` pass while
//! demonstrating its defects; the commented-out lines in `b`'s tests show
//! the calls that no longer compile.

pub mod a;
pub mod b;
